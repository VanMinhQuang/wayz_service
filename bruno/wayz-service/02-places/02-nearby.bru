meta {
  name: Find nearby places
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/places/nearby?lat=10.7769&lng=106.7009&radius=5000
  body: none
  auth: none
}

tests {
  test("Returns cursor list", function() {
    expect(res.status).to.equal(200);
    expect(res.body.data).to.be.an("array");
  });
}
