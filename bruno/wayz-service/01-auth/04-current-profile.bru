meta {
  name: Current profile
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/auth/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Profile is authenticated", function() {
    expect(res.status).to.equal(200);
  });
}
